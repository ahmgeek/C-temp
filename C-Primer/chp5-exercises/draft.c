#include <stdio.h>

int main(void)
{
  /*For play ground > printf() in the initialization expression ^_^
  int num;
  for(printf("Keep entering Numbers !\n"); num != 6; )
    scanf("%d",&num);

  printf("That's the one I want \n");
*/
//#
//#    const int ROWS = 6;
//#    const int CHARS = 6;
//#    int row;
//#    char ch;
//#    for (row = 0; row < ROWS; row++)
//#    {
//#      for (ch = ('A' + row); ch < ('A' + CHARS); ch++)
//#        printf("%c", ch);
//#      printf("\n");
//#    }
//#
  printf("size of int:%d\n",sizeof(size_t));
  return 0;
}
